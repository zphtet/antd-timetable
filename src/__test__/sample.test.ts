

import { describe, it, expect } from "vitest";

describe("sample", () => {
  it("should be true", () => {
    expect(true).toBe(true);
  });
});

describe("sample2", () => {
  it("should be true", () => {
    expect(true).toBe(true);
  });
});